@using PixelCreator.Content
@{
    ViewData["Title"] = "Fusion";
    Layout = "~/Views/Shared/_SharedUserView.cshtml";
}

<div class="row mb-3">
    <div class="col-12 text-center">
        <h2 class="heading">@PageHeaders.Fusion</h2>
    </div>
</div>
<section class="site-section">
<div class="container">
    @{
        // Define an array of image names for Fusion
        string[] imageNames = new string[] { "Fusion_Album_1.jpg", "Fusion_Album_2.jpg", "Fusion_Album_3.jpg" };
        // Define an array of links for each header
        string[] headerLinks = new string[] {
            Url.Action("FusionLayout1", "AlbumDesign")?? "",
            Url.Action("FusionLayout2", "AlbumDesign")?? "",
            Url.Action("FusionLayout3", "AlbumDesign")?? ""
        };
        // Update text headers array for Fusion
        string[] textheaders = new string[] { "Fusion" };
    }

    <div class="row justify-content-center">
        @for(int i = 0; i < imageNames.Length; i++)
        {
            <div class="col-md-4 mb-3 text-center">
                <div>
                    <!-- Thumbnail with clickable link -->
                    <a href="@headerLinks[i]">
                        <!-- Image preview -->
                        <img src="/images/Thumbnails/Sub_Album_Thumbnail/Fusion_Album_Thumbanails/@imageNames[i]" class="img-fluid img-thumbnail" alt="Image @(i + 1)">
                    </a>
                    <!-- Text header -->
                    <h5 class="mt-2"><a href="@headerLinks[i]" style="color: black; font-weight: bold;">@Html.Raw(textheaders[0])</a></h5>
                </div>
            </div>
        }
    </div>
</div>
</section>
